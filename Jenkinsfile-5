pipeline {
    agent {
        dockerfile {
            filename 'Dockerfile' // Default is 'Dockerfile', specify only if different
        }
    }
    stages {
        stage('Build and Run') {
            steps {
                echo 'Running inside the Docker container built from Dockerfile!'
                sh 'whoami && pwd && ls -l'
            }
        }
    }
}
